# 쿠팡 제휴 제품 연결 가이드 (Phase 2)

추천 성분을 실제 제품 구매로 연결해 **수수료 수익**을 만드는 기능입니다.
제품을 `data/products.json` 한 곳에서 관리하면, **성분 상세 페이지**와 **진단 결과 화면** 양쪽에 자동 노출됩니다.

## 작동 방식

```
data/products.json 에 제품 등록 (성분·피부타입 태그 + 쿠팡 링크)
        ↓ python generate_page.py
성분 상세 페이지: "이 성분이 든 추천 제품" 섹션
진단 결과 화면: "당신에게 맞는 추천 제품" 섹션
```

- 제품의 `ingredients` 태그 → 그 성분 상세 페이지에 노출
- 제품의 `skin_types` 태그 → 그 피부타입 진단 결과에 노출
- `status`가 `"active"`인 제품만 노출됩니다. 빈 상태에선 제품 섹션이 아예 안 나타납니다.

## 어느 제휴처를 쓸까

화장품 성분 사전에는 **올리브영 쇼핑 큐레이터**가 가장 잘 맞습니다. 여러 곳을 섞어 써도 됩니다.

| 제휴처 | 특징 | 자동 수집 |
|---|---|---|
| **올리브영 쇼핑 큐레이터** (추천) | 화장품 전문, 앱에서 바로 가입, 최대 7% 수익 | ✗ (앱에서 수동 링크 생성) |
| **쿠팡 파트너스** | 품목 다양, 공식 API로 자동 수집 가능 | ○ (`coupang_fetch.py`, 승인 후) |
| **링크프라이스** | 국내 제휴 1위, 다양한 브랜드 광고주 | 광고주별 상이 |

### 올리브영 쇼핑 큐레이터로 시작하기 (가장 빠름)

1. **올리브영 앱** → 마이페이지 등에서 '쇼핑 큐레이터' 활동 시작 (약관 동의, 누구나 가능)
2. 추천하려는 상품 페이지에서 **공유 링크(고유 URL) 생성**
3. 그 링크를 `data/products.json`의 `url`에 붙여넣고, `source`를 `"올리브영"`으로 설정
4. `status`를 `"active"`로 변경 → 성분 상세·진단 결과에 자동 노출

> 링크 클릭 후 24시간 내 구매 시 추천 상품 7%, 다른 상품 3% 수익. 공개 API는 없어 상품 등록은 수동입니다.

### 쿠팡 파트너스 (승인 후, 자동 수집 가능)

1. partners.coupang.com 에서 가입·승인 (무료)
2. 제품 페이지에서 **파트너스 링크 생성** → 추적코드가 붙은 URL 복사, `source`를 `"쿠팡"`으로
3. 또는 승인 후 `coupang_fetch.py`로 자동 수집 (아래 참고)

## 제품 등록하는 법

`data/products.json`의 `products` 배열에 제품을 추가하거나, 들어 있는 예시를 수정하세요.

```json
{
  "id": "고유한-영문-id",
  "name": "제품명 (브랜드 + 제품)",
  "url": "여기에_제휴_링크_붙여넣기",
  "image": "제품 이미지 URL (선택, 비워도 됨)",
  "price": "1만원대 (선택)",
  "source": "올리브영",                ← 제휴처 (카드에 출처 배지로 표시)
  "note": "한 줄 설명 (선택)",
  "ingredients": ["niacinamide"],     ← 어떤 성분 페이지에 보일지 (성분 id)
  "skin_types": ["지성", "복합성"],    ← 어떤 피부타입 결과에 보일지
  "status": "active"                   ← 링크 채운 뒤 active로 변경
}
```

**성분 id 찾는 법:** 각 성분 상세 페이지 파일명이 곧 id입니다.
예) `detail/niacinamide.html` → id는 `niacinamide`. `data/ingredients.json`에서도 확인 가능합니다.

**피부타입 값:** `건성`, `지성`, `민감성`, `복합성`, `여드름성` 중에서 입력.

## 등록 후 반영

```bash
python generate_page.py --base-url https://merry-gelato-2a6bc1.netlify.app/
git add -A && git commit -m "제휴 제품 등록" && git push
```
push하면 Netlify가 자동 재배포합니다.

## ⚠️ 꼭 지킬 것

- **제휴 고지 의무:** 쿠팡 파트너스는 "수수료를 받는다"는 문구 표기가 필수입니다.
  → 이미 `products.json`의 `affiliate_notice`에 문구가 들어 있고, 제품 섹션마다 자동 표시됩니다. 지우지 마세요.
- **링크 속성:** 제휴 링크는 검색엔진 가이드상 `rel="nofollow sponsored"`로 자동 처리됩니다 (코드에 반영됨).
- **과장 광고 주의:** 제품 설명(`note`)에 의학적 효능(치료·완치 등)을 단정하지 마세요.
- 처음엔 성분당 1~2개씩, 정말 추천할 만한 제품만 등록하는 게 신뢰도에 좋습니다.

## 자동으로 채우기 — 쿠팡 파트너스 API (`coupang_fetch.py`)

제품을 일일이 손으로 넣지 않고, **쿠팡 파트너스 공식 API**로 성분별 추천 상품을
자동으로 가져와 채울 수 있습니다. (쿠팡 직접 크롤링은 약관 위반이고, 추적코드가 없어
수익이 안 됩니다. 반드시 공식 API를 씁니다.)

**1. API 키 발급**
파트너스 로그인 → 상단 **Tools → 파트너스 API** → ACCESS KEY / SECRET KEY 발급

**2. 실행** (본인 컴퓨터에서)
```bash
pip install requests --break-system-packages
export COUPANG_ACCESS_KEY=발급키
export COUPANG_SECRET_KEY=발급시크릿

# 기본 대표 성분 20종, 성분당 2개씩 가져오기
python coupang_fetch.py --per 2

# 특정 성분만
python coupang_fetch.py --ingredients niacinamide retinol centella --per 3
```

**3. 동작**
- 성분 한글명으로 쿠팡 상품을 검색해, **추적코드가 붙은 링크**·이미지·가격을 가져옵니다
- `products.json`에 `status:"draft"`로 추가합니다 (기존 항목은 보존)
- 검토 후 좋은 제품만 `status:"active"`로 바꾸면 사이트에 노출됩니다
  (`--activate` 옵션을 주면 바로 active로 들어오지만, 부적합 상품이 섞일 수 있어 검토 권장)
- 피부타입 태그는 해당 성분에 '추천(good)'인 타입으로 자동 설정됩니다

**4. 반영**
```bash
python generate_page.py --base-url https://merry-gelato-2a6bc1.netlify.app/
git add -A && git commit -m "제휴 제품 추가" && git push
```

> 주 1회 자동 실행을 걸어두면(예약 작업 또는 cron) 신상품으로 꾸준히 갱신됩니다.
> 다만 노출 전 검토 단계는 유지하는 것이 신뢰도에 좋습니다.

## 다음 단계

어떤 성분·제품이 실제로 클릭·구매되는지 쿠팡 파트너스 리포트로 확인하면,
그게 **자체 상품 기획(Phase 3~4)** 의 가장 좋은 근거가 됩니다.
