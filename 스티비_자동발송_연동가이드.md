# 스티비(Stibee) 자동 발송 연동 가이드

진단 결과 이메일을 받으면 **피부타입별 맞춤 리포트가 자동으로 발송**되도록 설정하는 방법입니다.
현재 Netlify Forms는 이메일을 모으기만 하므로, 자동 발송을 위해 스티비를 연결합니다.

## 전체 그림

```
퀴즈 진단 → 이메일 입력 (피부타입 함께 전송)
        ↓
스티비 주소록에 구독자 추가 + 피부타입별 그룹 자동 분류
        ↓
그룹별 '자동 이메일'이 즉시 발송 (건성/지성/민감성/복합성/여드름성 리포트)
```

## 1단계. 스티비 가입 & 주소록 만들기
1. stibee.com 가입 (무료 플랜으로 시작 가능)
2. **주소록** 생성 (예: "성분 사전 구독자")
3. 주소록 안에 **그룹 5개** 생성: 건성 / 지성 / 민감성 / 복합성 / 여드름성

## 2단계. 자동 이메일(웰컴 리포트) 설정
1. 스티비 **자동 이메일** 메뉴 → "주소록에 구독자가 추가됐을 때" 트리거 선택
2. 그룹별로 자동 이메일 5개를 만들고, 각 그룹에 해당하는 리포트를 넣습니다
   - 리포트 본문은 `피부타입별_리포트_초안.md`의 5종을 복사해 사용하세요
   - 본문 속 `{분석기 링크}` 등은 실제 페이지 주소로 교체:
     - 전성분 분석기: `https://(도메인)/analyzer.html`
     - 궁합 체크: `https://(도메인)/combos.html`
     - 상황별 회피: `https://(도메인)/avoid.html`
3. 발송 조건을 "해당 그룹에 추가된 구독자"로 지정 → 타입별로 맞는 리포트만 자동 발송

## 3단계. 퀴즈 폼을 스티비로 연결
두 가지 방법이 있습니다.

**(A) 스티비 구독 폼 직접 사용 — 가장 간단**
- 스티비에서 발급한 구독 폼 HTML을 퀴즈 결과 영역에 넣습니다
- 피부타입은 구독 폼 URL 파라미터로 그룹을 지정할 수 있습니다
  (스티비: "구독 폼 URL 파라미터로 구독자를 그룹으로 자동 분류" 기능)

**(B) 지금 폼을 유지하고 스티비로 전송 — 디자인 유지**
- 현재 퀴즈의 폼 디자인을 그대로 두고, 제출만 스티비 주소록 API/폼 액션으로 보냅니다
- 이 방식으로 하려면 아래 정보가 필요해요. 저에게 알려주시면 퀴즈 폼을 맞춰 연결해 드립니다:
  1. 스티비 **주소록 고유 코드**(또는 구독 폼 action URL)
  2. 피부타입 → 그룹을 매칭할 **그룹 코드/이름**
  3. 이메일·그룹을 받는 **필드명**

## 참고
- 스티비는 한글 서비스라 설정이 쉽고, 무료 플랜으로도 자동 이메일을 쓸 수 있습니다(발송량 한도 확인).
- '구독 확인 이메일'을 켜면 동의 기반(더블 옵트인)으로 더 안전하게 운영할 수 있어요.
- Netlify Forms는 백업 명단으로 함께 둬도 됩니다.

준비되면 위 (B)의 3가지 정보를 알려주세요 — 퀴즈의 "내 피부 맞춤 리포트 받기" 폼을 스티비로 바로 연결해 드릴게요.
